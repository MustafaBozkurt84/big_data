1. We can produce messages to a kafka topic with many differet ways. One of them is Python.

2. In play folder you will see kafka_producer.py

3. `(venvspark) [train@localhost play]$ python kafka_producer.py` 